# 请求详细日志与镜像构建实施记录

## 技术栈与边界

- 后端：Go、Gin、GORM v2；保持 SQLite、MySQL、PostgreSQL，兼顾可选 ClickHouse 日志库。
- 前端：React、TypeScript、TanStack Router、React Query、现有 usage-logs 组件。
- 部署：仓库现有 `Dockerfile`，根目录 `deploy.sh --build` 构建镜像，`deploy.sh --dev` 复用镜像并挂载本地 Linux/amd64 运行文件。
- 请求详情只在普通使用日志链路中落库；详情正文必须限长、脱敏，写入失败不得阻断计费/转发。
- 详情关联使用独立稳定 `detail_id`，不能依赖 ClickHouse 场景下的分页展示 `logs.id`。

## 实施顺序

1. 确认数据库初始化、日志库选择和 relay 请求/响应生命周期。
2. 先测试后实现请求详情模型、脱敏/截断、关联写入和权限 API。
3. 接入前端列表列、详情路由和 Postman 风格预览。
4. 新增 `deploy.sh --build`，执行脚本语法和真实镜像验证。
5. 执行后端、前端、差异和收尾审计。

## 当前根因/风险锚点

- `model.Log.Id` 在 ClickHouse 下可能由列表层按分页序号赋值，不能作为跨请求稳定详情主键。
- 请求 body 在不同 relay/streaming 分支的可见时机不同，必须以实际统一生命周期为准；不能在 provider adapter 中复制采集逻辑。
- 请求/响应可能携带 Authorization、Cookie、query token、二进制和超大上下文，详情采集默认采用安全降级。

## 验证门槛

- 后端新增行为遵循 RED -> GREEN -> REFACTOR，并运行受影响 Go 测试。
- 前端修改后运行受影响 Vitest、`bun run typecheck`、lint 和 `bun run build:check`。
- `deploy.sh` 运行 `bash -n`，并在可用 Docker daemon 上真实构建 `cqingwang/litellm:latest`。
- 完成后检查 `git diff --check`、工作树范围和 README/受保护标识未被无关修改。

## 已完成证据

- 已新增 `model.RequestLogDetail`、`logs.detail_id`、详情查询 API 和统一 relay 响应捕获。
- 已新增使用日志详细信息列、独立详情路由与请求/响应预览页，并同步七种前端语言资源。
- `go test ./...` 通过；usage-logs 前端测试 2 个文件/7 个用例通过；`bun run typecheck`、`bun run build:check`、`bash -n deploy.sh` 和 `git diff --check` 通过。
- 全量前端 lint/format 检查仍受仓库既有错误影响；定向检查本次新增文件未发现新增错误，`api.ts` 保留已有 `api.ts` 与 `lib/utils.ts` 循环告警。
- `./deploy.sh --build` 已执行但当前主机 Docker daemon 不可用，镜像生成尚未取得真实运行证据。

## --dev 实施证据

- `deploy.sh --dev` 已按源码时间戳准备 `.dev/new-api`，使用 `cqingwang/litellm:latest` 启动 `litellm-dev`。
- 容器实际挂载 `.dev/new-api:/new-api:ro` 与本地 `data:/data`，`/api/status` 返回 `success=true`。
- 当本地前端/Go 源码和前端产物均未变化时，脚本复用已有本地编译产物；已有同名调试容器时先删除再重新启动，数据由本地 `data` 挂载保留。

## 详情入口点击修复

- 根因：使用日志列表在 React Query 后台刷新已有数据时传入 `isFetching=true`，公共表格组件因此给桌面/移动表格容器增加 `pointer-events-none`；刷新期间详情链接收不到点击事件，表现为等待几秒后才有效。
- 修复：`DataTablePage` 新增 `disableInteractionWhileFetching`，默认保持原有禁用行为；使用日志页关闭该锁，使详情等只读导航不受后台刷新阻塞。
- 回归：新增 `data-table-page.test.tsx`，覆盖显式关闭交互锁和默认保持交互锁两种契约。

## 详情页紧凑布局

- 标题、日志 ID、复制按钮和 Request/Response Tabs 合并到同一行；方法、状态、耗时、路径压缩到下一行。
- Request/Response 内部统一采用“Headers 在上、Body 在下”的垂直布局；Headers 使用 Accordion 折叠，减少长请求头占用的可视空间。

## 清理问题修复

- “清理日志文件”仅删除 `common.LogDir` 下的文本日志文件，不影响使用日志数据库中的 `logs` 表。
- “清理日志”通过系统任务异步删除 `LOG_DB.logs` 中 `created_at < target_timestamp` 的记录；任务成功后前端原先未失效 `logs` 查询缓存，导致页面继续显示旧数据。
- 修复为任务成功后刷新使用日志和统计查询，并在任务完成时清理主数据库中的历史 `request_log_details`，避免新增详情表产生孤儿记录。
- 请求/响应体改为保留采集到的原始字符串，仅由代码块组件按 JSON 或纯文本进行语法展示，不改变复制内容。
- `text/event-stream` 响应按 SSE 行结构保真展示，仅对 `data:` 后可解析的 JSON 片段着色；事件名、前缀、换行、空行及 `[DONE]` 等内容不改写。
- 当前调试 SQLite 证据：`logs.created_at` 使用 Unix 秒；现有记录均为当前时间附近，选择“30 天前”时返回无匹配是预期行为。日志文件清理确认框已改为受控状态，成功后主动关闭，失败时保持打开。
- 历史日志清理默认时间改为当前时间，避免默认“30 天前”导致当前日志始终不会被清理；用户仍可通过时间选择器或快捷选项缩小清理范围。
