# deploy.sh 与请求日志查询优化计划

## 目标

参考 `../LiteLLM/deploy.sh` 的命令风格，在当前 LiteLLM 项目根目录提供：

- `./deploy.sh --build`：构建当前项目 Docker 镜像。
- `./deploy.sh --dev`：构建本地开发运行文件与前端静态资源，并通过 Docker 启动开发容器。

## 已锚定的项目事实

- Docker 构建入口是根目录 `Dockerfile`，最终服务监听 8090。
- 前端使用 `pnpm build`，产物需要位于 `internal/server/static/dist` 才能被 `internal/server/static/embed.go` 的 Go `embed` 编入运行文件。
- 后端入口是 `./cmd/axonhub`，构建标签使用 `nomsgpack`。
- 默认数据库是 SQLite；开发容器将 `/data/axonhub.db` 作为持久化数据库。
- 运行镜像使用 `scratch`；镜像构建前由本机生成前端静态资源、Linux/amd64 二进制和 CA 证书，Docker 阶段只负责打包这些文件，时区数据由 `cmd/axonhub/main.go` 的 `time/tzdata` 嵌入提供。

## 实现决策

- 镜像标签固定为 `litellm:latest`。
- 目标平台固定为 `linux/amd64`，开发构建使用 `amd64`。
- 开发容器名默认为 `axonhub-dev`，端口默认为 8090，分别可通过 `AXONHUB_DEV_CONTAINER` 与 `AXONHUB_DEV_PORT` 覆盖。
- 启动前删除同名旧容器，保证重复执行 `--dev` 时状态确定。
- 复用项目已有的前端依赖与 Go 构建方式；部署脚本直接调用已安装的 Vite，避免 `pnpm` 在构建时隐式触发依赖安装。不执行测试、lint 或构建验证，避免脚本新增任务越过仓库现有约束。

## 验证

- 使用 `bash -n deploy.sh` 验证 Shell 语法。
- 使用 `./deploy.sh --help` 验证参数帮助路径，不启动 Docker 服务。
- 检查 Dockerfile 不再引用 Node、Go 或 Alpine 基础镜像。

## 请求日志查询优化

### 根因

- 请求列表的 GraphQL 字段没有请求日志体，但 Ent 查询未显式 `Select`，会默认读取 `requests` 的全部列。
- 列表中嵌套的 `request_executions` 同样默认读取请求体、响应体、响应分片和请求头。
- 详情查询通过 `node(id:)` 及执行记录详情字段加载日志体，不能用全局屏蔽字段的方式处理。

### 实现决策

- 列表查询按 GraphQL 选择集只选择非日志体字段。
- 当 GraphQL 选择集中出现请求体、响应体、响应分片或请求头字段时，恢复对应实体的完整列，保证详情查询兼容。
- 请求列表和执行记录列表共用同一套字段选择规则，避免只优化外层查询而继续加载嵌套大字段。

### 验证

- 增加字段选择回归测试，确保轻量查询不包含日志体字段，详情查询包含完整字段。
- 遵循仓库规则，不在本次变更中执行构建、测试或启动服务。

## 索引审计

### 已覆盖的关键路径

- `requests` 已覆盖 API Key、项目、渠道、Trace 与 `created_at` 的组合索引，适配请求列表的筛选和时间排序。
- `request_executions` 已覆盖请求与 `created_at`、请求与状态及 `created_at`，适配详情执行记录分页和最新执行记录查询。
- `usage_logs` 已覆盖请求关联、时间、模型、项目、渠道和 API Key 的查询路径，适配日志列表及统计分析。

### 风险记录

- TPS 使用的 `(status, metrics_latency_ms)` 索引只存在于 `internal/ent/migrate/migrations/20260207143000_add_tps_indexes.sql`，未出现在 Ent schema 及运行时迁移注册中。该索引不影响请求日志列表，但在生产环境未单独执行该 SQL 时，TPS 统计可能退化为扫描。
- 本次不新增重复索引；若确认该 SQL 未由发布流程执行，应将其纳入正式迁移流程，或在 Ent schema 中补齐后重新生成迁移代码。

## 请求详情共享授权

### 根因

- 前端没有独立的 session ID，当前登录态实际由 `axonhub_access_token` 访问令牌维护。
- 仅复制 `/project/requests/:id` 路径到另一个智能体或浏览上下文时，目标环境无法从原上下文恢复授权。

### 实现决策

- 请求详情页使用 `sessionid` 查询参数承载当前访问令牌，例如 `?sessionid=<access-token>`。
- 进入请求详情页时自动将当前令牌补到 URL，保留已有查询参数。
- 应用启动时从 `sessionid` 恢复访问令牌，继续复用现有 GraphQL Authorization 头逻辑。
- 服务端根据 `Accept: text/html` 保留浏览器 SPA 导航；非 HTML 客户端携带 `sessionid` 时直接返回 JSON，避免网页刷新被误判为分享接口。
